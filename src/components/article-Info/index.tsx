

type Props = {
    author: string;
    postDate: string;

    readingTime: number;

};

const ArticleInfo = ({ author, postDate, readingTime }: Props) => {

    return (
        
    )
}

export default ArticleInfo;